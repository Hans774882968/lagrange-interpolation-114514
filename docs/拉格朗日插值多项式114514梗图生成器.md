## 提示词（通义千问网页端）

```
大佬，你是一名专家前端工程师，精通前端工程化。请你帮我写一个html文件，支持输入一个数组，在结果展示区域展示类似于这张图片的内容，并支持导出图片。
具体需求：
（1）网页支持输入数组。数据格式：一行字符串，以空格隔开，问号表示要填空的数。如果有1个问号，那么我希望问号里的数填114514，第2个问号填1919810，第3个问号填114154……以此类推。比如1,3,5,?,9,?,13，那么需要展示的拉格朗日插值多项式需要过这些点：
1,1
2,3
3,5
4,114514
5,9
6,1919810
7,13
这里需要响应式地展示拉格朗日插值的多项式和图片中的其他元素
（2）支持点击“保存图片”按钮，对结果展示区域截图并保存图像。用html2canvas实现就OK
要求：
1. 技术栈：React+Tailwind CSS
2. 输出单个html文件
在写代码之前，你可以先问我一些问题
```

根据LLM的提问，我回答：

```
1. 两种都要支持
2. 需要精确的分数形式。请自行决定使用什么符号计算包能够比较方便地进行多项式加法、乘法和多项式展开。并且结果展示区域需要展示公式的latex格式
3. 数字序列是114514和1919810的交替，所以第4个问号表示的数字是1919810
4. 背景色和文字颜色用tweakcn的quantum-rose主题的
5. 是的
另外强调一下，图片中“真有逻辑”“哇 数学真是有趣”的文字也要有
大佬，请开始写代码
```

有严重bug，而且主题也没有用quantum-rose。

```
大佬：
1. 一进入页面就提示输入格式错误，请检查，并未正常显示结果展示区域。请检查代码里的bug
2. quantum-rose主题的CSS代码请用这个：
```

现在就OK了。但还有一些不符合需求的细节，以及其他修改：

```
谢谢大佬！接下来请在我给你的代码的基础上做以下改动：
1. 希望在输入框按回车相当于点击“生成多项式”按钮
2. “用空格或逗号分隔， ? 表示待填空的数”这条注释改为一个圆圈里带问号的icon，鼠标悬浮时显示提示，文案为：“用空格或逗号分隔， ? 表示待填空的数。逗号和问号字符支持中文和英文”
3. “保存图片”文案的右边（按钮内）新增一个圆圈里带问号的icon，鼠标悬浮时显示提示，文案为：“html2canvas 保存出来的图片错位严重，建议按 F12 打开控制台，用 capture node screenshot 命令来截图”
4. 结果展示区域，以`f(4)=114514`为例，在外面加一个圆角矩形框住。但`f(2)=3`等不是问号的函数值不需要加框
5. 结果展示区域的“真有逻辑”不出现在函数值的右边，改成只出现1个，且是绝对定位的

请在这段代码的基础上修改：

// 完整代码
```

改得不错。然后我又手动加了张绝对定位的img，导致保存图片功能报错`SecurityError: Failed to execute 'toDataURL' on 'HTMLCanvasElement': Tainted canvases may not be exported`

```
谢谢大佬！我在结果展示区域加了一张绝对定位的图片`<img src="野兽先辈.png" alt="野兽先辈" className="absolute bottom-8 right-8" />`。发现图片正常显示，但点击“保存图片”按钮后，控制台报错`SecurityError: Failed to execute 'toDataURL' on 'HTMLCanvasElement': Tainted canvases may not be exported`。如果我给这个img标签加上`crossOrigin="anonymous"`，“保存图片”按钮就能正常执行，但是看不到图片。

我希望把这个html文件部署到GitHub Pages上，项目名是`lagrange-interpolation-114514`，预期的访问网址是`https://hans114514.github.io/lagrange-interpolation-114514/拉格朗日插值多项式114514梗图生成器.html`。请问图片应该放置在项目的哪个位置，html引用图片的代码怎么写，以及后续如何把这个开源项目部署到GitHub Pages？
```

根据LLM的提示，不改动代码，仅仅`Open with Live Server`就能解决这个问题。

顺便确定部署方案：

GitHub选择用main分支部署，为此，我们把文件名改为`index.html`。不需要GitHub Actions

TODO: 移动端适配
